<launch>

  <!-- This file makes it easy to include the settings for sensor managers -->
  <arg name="robot_model" default="locobot_wx250s"/>
  <arg name="robot_name" default="locobot"/>
  <arg name="use_camera" default="true"/>

  <!-- Params for the octomap monitor -->
  <!--  <param name="octomap_frame" type="string" value="some frame in which the robot moves" /> -->
  <!-- <param name="octomap_resolution" type="double" value="0.025" />
  <param name="max_range" type="double" value="5.0" /> -->

  <group if="$(arg use_camera)">
    <!-- Params for 3D sensors config -->
    <!-- <rosparam command="load" file="$(find interbotix_xslocobot_moveit)/config/sensors_3d.yaml" /-->
    <!-- <rosparam command="load" file="$(find moveit_custom)/config/sensors_3d_depth_map.yaml" />  -->
    <!-- <rosparam command="load" file="$(find moveit_custom)/config/sensors_3d_point_cloud.yaml" /> -->
    <!-- Load the robot specific sensor manager; this sets the moveit_sensor_manager ROS parameter -->
    <include file="$(find moveit_custom)/launch/octomap_mapping.launch"> </include> 
    <rosparam command="load" file="$(find moveit_custom)/config/sensors_3d_point_cloud.yaml" />
  </group>

</launch>
